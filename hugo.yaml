# Hugo configuration file
title: бюро {ландушевої}

# import hextra as module
module:
  imports:
  - path: github.com/imfing/hextra

markup:
  # allow raw html
  goldmark:
    renderer:
      unsafe: true

  # enable hextra syntax highlight
  highlight:
    noClasses: false

menu:
  main:
    - name: Портфоліо
      pageRef: /portfolio
      weight: 1
    - name: Про нас
      pageRef: /about
      weight: 2
    - name: Контакти
      pageRef: /contact
      weight: 3
    - name: Instagram
      weight: 4
      url: "https://www.instagram.com/nastialandusheva/"
      params:
        icon: instagram

params:
  navbar:
    displayTitle: true
    displayLogo: true
    logo:
      path: images/logo.svg
      dark: images/logo.svg
      link: /
      width: 120
      height: 60
    displayAnchoredHeadings: false

  page:
    # full (100%), wide (90rem), normal (1280px)
    width: full

  footer:
    displayCopyright: false
    displayPoweredBy: false

  editURL:
    enable: true
    base: "https://github.com/imfing/hextra-starter-template/edit/main/content"
